# Base dependencies are installed from gendoc-template/requirements.txt.
# This file contains only project-specific additions not in the template.
#
# Install: pip install -r gendoc-template/requirements.txt -r requirements.txt

mkdocs-redirects==1.2.1
