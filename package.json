{
  "name": "gnus-ai-docs",
  "version": "1.0.0",
  "private": true,
  "description": "GNUS.AI documentation — MkDocs site deployed to Cloudflare Pages",
  "scripts": {
    "deploy": "wrangler pages deploy site --project-name=gnus-ai-docs",
    "preview": "wrangler pages dev site"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}

