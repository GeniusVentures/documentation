---
phase: 07-cloudflare-pages-deploy-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gendoc-template/scripts/deploy.sh
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "All .json files in site/ are gzipped to .json.gz and raw .json deleted pre-deploy — no special cases, no _headers file"
    - "No in-place gzip of search_index.json — it follows the same uniform .json → .json.gz pattern as every other file"
    - "After wrangler deploy, all raw .json files are restored from .json.gz for local development compatibility"
  artifacts:
    - path: "gendoc-template/scripts/deploy.sh"
      provides: "Uniform JSON gzip and deploy orchestration"
      min_lines: 80
  key_links:
    - from: "gendoc-template/scripts/deploy.sh"
      to: "gendoc.yml deploy.cloudflare section"
      via: "python3 yaml read"
      pattern: "deploy.*cloudflare.*branch"
    - from: "gendoc-template/scripts/deploy.sh"
      to: "wrangler pages deploy"
      via: "--branch flag"
      pattern: "wrangler pages deploy --branch"
---

<objective>
Rewrite `deploy.sh` to implement uniform JSON gzip (D-01), remove the special-case in-place gzip + `_headers` pattern for `search_index.json` (D-02), and add gendoc.yml-driven configuration for the deploy branch and gzip toggle (D-03 initial, D-05). All `.json` files in the site directory follow the same pattern: gzip to `.json.gz`, delete raw `.json`. The `_headers` file is no longer written.

Purpose: Eliminates the fragile two-strategy approach (in-place gzip for one file, .json.gz for others). Every file is handled identically. The gzip_json toggle controls whether the gzip/delete/restore cycle runs — when false, deploy.sh skips it entirely.
Output: A single, updated `gendoc-template/scripts/deploy.sh` with uniform gzip, YAML-driven config, and search SW staging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-CONTEXT.md
@gendoc-template/scripts/deploy.sh
@gendoc-template/ask-ai/widget-src/config.ts

<interfaces>
The deploy.sh script currently:
- Writes a `_headers` file for Cloudflare transparent gzip serving (lines 56-62) — REMOVE
- Gzips `search_index.json` in-place via `gzip -c | mv` to keep the same filename (lines 67-72) — REMOVE
- Loops over other `.json` files, gzips to `.json.gz`, deletes raw `.json` (lines 77-81) — MODIFY to include ALL .json
- Deploys with hardcoded `--branch main` (line 87) — REPLACE with gendoc.yml value
- Restores `search_index.json` from `.raw.bak` (lines 103-106) — REMOVE this special case
- Restores other `.json` from `.gz` (lines 109-112) — KEEP but include search_index.json.gz uniformly
- Reads `PROJECT_NAME` from gendoc.yml via `python3 -c "import yaml..."` (lines 43-48) — existing YAML-read pattern to follow

The gzip magic-byte + DecompressionStream pattern from `config.ts` lines 39-46 is authoritative for the search SW created in Plan 02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite gzip section for uniform handling (D-01, D-02)</name>
  <files>gendoc-template/scripts/deploy.sh</files>
  <read_first>gendoc-template/scripts/deploy.sh</read_first>
  <action>
Remove the _headers write block (lines 56-62 — the entire `cat > "$SITE_DIR/_headers"` through `echo "  Wrote _headers"`) and the in-place gzip block for search_index.json (lines 67-72 — from `SEARCH_INDEX=` through `echo "  Gzipped search_index.json in-place"`). Remove the `! -path "*/search/search_index.json"` exclusion from the find loop on line 81 so ALL .json files are handled uniformly. In the restore section (lines 98-116), remove the `.raw.bak` restore for search_index.json (lines 103-106) and the cleanup of `.raw.bak` on line 115 — the `.json.gz` → `.json` gunzip loop already covers search_index.json uniformly. Update the echo message at line 80 from `"  Gzipped and removed:"` to include a count: `echo "  Gzipped and removed $count .json files"`. Keep the `deploy_ok` pattern and exit code handling as-is. Keep the PROJECT_NAME display (lines 43-50) — do not modify it.
  </action>
  <acceptance_criteria>
    - `grep -c "_headers" gendoc-template/scripts/deploy.sh` returns 0 (or only in the echo messages/error output, not in a write or file creation context)
    - `grep -c "raw.bak" gendoc-template/scripts/deploy.sh` returns 0
    - `grep -c "search_index.json" gendoc-template/scripts/deploy.sh` returns 0 (the file should not be singled out anywhere)
    - The find loop now uses `find "$SITE_DIR" -name "*.json" -type f ! -name "*.json.gz"` without any `-path` exclusion for search_index
    - The restore loop uses `find "$SITE_DIR" -name "*.json.gz" -type f` and applies `gunzip -kf` to all — no special search_index.json handling
    - `grep -c "wrangler pages deploy" gendoc-template/scripts/deploy.sh` returns exactly 1
  </acceptance_criteria>
  <verify>
    <automated>bash -n gendoc-template/scripts/deploy.sh && echo "Syntax OK"</automated>
  </verify>
  <done>deploy.sh contains no _headers write, no raw.bak, no search_index.json special case, and all .json files are handled uniformly in the gzip loop and restore loop</done>
</task>

<task type="auto">
  <name>Task 2: Add gendoc.yml-driven config and SW copy (D-03 initial, D-05)</name>
  <files>gendoc-template/scripts/deploy.sh</files>
  <read_first>gendoc-template/scripts/deploy.sh</read_first>
  <action>
After the PROJECT_NAME read block (line 48), add YAML reads for two new values using the same `python3 -c "import yaml..."` pattern:

1. `DEPLOY_BRANCH` — reads `deploy.cloudflare.branch` from gendoc.yml, defaults to `"main"`:
```text
DEPLOY_BRANCH=$(python3 -c "
import yaml, sys
with open(sys.argv[1], 'r') as f:
    cfg = yaml.safe_load(f)
print(cfg.get('deploy', {}).get('cloudflare', {}).get('branch', 'main'), end='')
" "$GENDOC_YML")
```

2. `GZIP_JSON` — reads `deploy.cloudflare.gzip_json` from gendoc.yml, defaults to `true`:
```text
GZIP_JSON=$(python3 -c "
import yaml, sys
with open(sys.argv[1], 'r') as f:
    cfg = yaml.safe_load(f)
print(cfg.get('deploy', {}).get('cloudflare', {}).get('gzip_json', True), end='')
" "$GENDOC_YML")
```

Wrap the entire gzip/delete loop and the restore block inside `if [ "$GZIP_JSON" = "True" ]; then ... fi`. When `GZIP_JSON` is not True, echo a message: `"Skipping JSON gzip (deploy.cloudflare.gzip_json is not True)"`. The if block should start right after the `echo "Preparing files for deployment..."` line and end before the `# ── Deploy using the wrangler.toml` comment. When gzip is skipped, the deploy still runs — only the gzip/restore cycle is bypassed.

**After the gzip if-block and before the wrangler deploy**, add a copy of the search service worker to site root so it has full scope for fetch interception:
```text
SW_SRC="$TEMPLATE_ROOT/javascripts/search-gzip-sw.js"
SW_DST="$SITE_DIR/search-gzip-sw.js"
if [ -f "$SW_SRC" ]; then
    cp "$SW_SRC" "$SW_DST"
    echo "  Copied search-gzip-sw.js to site root"
fi
```

Replace the hardcoded `--branch main` on the wrangler command line with `--branch "$DEPLOY_BRANCH"`.
  </action>
  <acceptance_criteria>
    - `grep "DEPLOY_BRANCH" gendoc-template/scripts/deploy.sh` finds the variable definition using `python3 -c "import yaml..."` with `deploy.cloudflare.branch` path
    - `grep "GZIP_JSON" gendoc-template/scripts/deploy.sh` finds the variable definition using `python3 -c "import yaml..."` with `deploy.cloudflare.gzip_json` path
    - The gzip/delete loop and the restore block are enclosed in `if [ "$GZIP_JSON" = "True" ]; then ... fi`
    - When `GZIP_JSON` is not True, an echo message about skipping gzip is emitted and neither gzip nor restore runs
    - `grep "search-gzip-sw.js" gendoc-template/scripts/deploy.sh` shows a copy from `$TEMPLATE_ROOT/javascripts/search-gzip-sw.js` to `$SITE_DIR/search-gzip-sw.js` with an existence check
    - `grep "wrangler pages deploy" gendoc-template/scripts/deploy.sh` shows `--branch "$DEPLOY_BRANCH"` (not `--branch main`)
    - The PROJECT_NAME read block is preserved unchanged
  </acceptance_criteria>
  <verify>
    <automated>bash -n gendoc-template/scripts/deploy.sh && echo "Syntax OK"</automated>
  </verify>
  <done>deploy.sh reads deploy.cloudflare.branch and deploy.cloudflare.gzip_json from gendoc.yml, wraps gzip/restore in the gzip_json toggle, copies search-gzip-sw.js to site root, and passes the branch to wrangler pages deploy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gendoc.yml → deploy.sh | User-controlled YAML values (branch, gzip_json) consumed by shell script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | deploy.sh YAML reads | mitigate | Python yaml.safe_load prevents YAML code execution; branch value is shell-quoted with double quotes; gzip_json is compared to literal "True" not truthy |
| T-07-02 | Information Disclosure | deploy.sh echo output | accept | File paths echoed are relative to TEMPLATE_ROOT which is already known via wrangler.toml; no secrets exposed |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — deploy.sh uses existing python3 + yaml, gzip, gunzip, find, cp (all system tools) |
</threat_model>

<verification>
- `bash -n gendoc-template/scripts/deploy.sh` passes syntax check
- Visual review: no `_headers`, no `raw.bak`, no `search_index.json` special case
- Two new YAML reads (`branch`, `gzip_json`) present using the same `python3 -c` pattern as `PROJECT_NAME`
- `--branch "$DEPLOY_BRANCH"` on the wrangler line
</verification>

<success_criteria>
deploy.sh is a single, unified script where all JSON files follow identical gzip→delete→restore logic, branch and gzip_json come from gendoc.yml, and the search SW file is staged at site root before deploy.
</success_criteria>

<output>
Create `.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-01-SUMMARY.md` when done
</output>
