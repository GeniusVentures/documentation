---
phase: 07-cloudflare-pages-deploy-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gendoc-template/scripts/deploy.sh
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "All .json files in site/ are gzipped to .json.gz and raw .json permanently deleted pre-deploy — no special cases, no _headers file"
    - "No in-place gzip of search_index.json — it follows the same uniform .json → .json.gz pattern as every other file"
    - "No post-deploy restore — .json.gz is the artifact. Local dev uses gzip_json: false or a fresh mkdocs build"
    - "fetch-gzip.js wrapper is NOT copied here — copy-assets.py handles javascripts/ → site/ automatically"
  artifacts:
    - path: "gendoc-template/scripts/deploy.sh"
      provides: "Uniform JSON gzip and deploy orchestration"
      min_lines: 70
  key_links:
    - from: "gendoc-template/scripts/deploy.sh"
      to: "gendoc.yml deploy.cloudflare section"
      via: "python3 yaml read"
      pattern: "deploy.*cloudflare.*branch"
    - from: "gendoc-template/scripts/deploy.sh"
      to: "wrangler pages deploy"
      via: "--branch flag"
      pattern: "wrangler pages deploy --branch"
---

<objective>
Rewrite `deploy.sh` to implement uniform JSON gzip (D-01), remove the special-case in-place gzip + `_headers` pattern for `search_index.json` (D-02), and add gendoc.yml-driven configuration for the deploy branch and gzip toggle (D-03, D-05). All `.json` files in the site directory follow the same pattern: gzip to `.json.gz`, delete raw `.json` permanently. No restore. The `_headers` file is no longer written.

Purpose: Eliminates the fragile two-strategy approach (in-place gzip for one file, .json.gz for others). Every file is handled identically. The gzip_json toggle controls whether the gzip/delete cycle runs — when false, deploy.sh skips it entirely and deploys raw `.json` as-is.

Output: A single, updated `gendoc-template/scripts/deploy.sh` with uniform gzip, YAML-driven config, and no restore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-CONTEXT.md
@gendoc-template/scripts/deploy.sh

<interfaces>
The deploy.sh script currently:
- Writes a `_headers` file for Cloudflare transparent gzip serving (lines 56-62) — REMOVE
- Gzips `search_index.json` in-place via `gzip -c | mv` to keep the same filename (lines 67-72) — REMOVE
- Loops over other `.json` files, gzips to `.json.gz`, deletes raw `.json` (lines 77-81) — MODIFY to include ALL .json
- Deploys with hardcoded `--branch main` (line 87) — REPLACE with gendoc.yml value
- Restores `search_index.json` from `.raw.bak` (lines 103-106) — REMOVE entirely (no restore)
- Restores other `.json` from `.gz` (lines 109-112) — REMOVE entirely (no restore)
- Cleans up temp files (line 115) — REMOVE entirely
- Reads `PROJECT_NAME` from gendoc.yml via `python3 -c "import yaml..."` (lines 43-48) — existing YAML-read pattern to follow

New behavior:
- When `gzip_json` is true: find all `.json`, gzip to `.json.gz`, delete raw `.json`, deploy. Done. No restore.
- When `gzip_json` is false: skip gzip entirely, deploy raw `.json` as-is. Log a message.
- fetch-gzip.js copying is NOT needed here — `copy-assets.py` already mirrors `javascripts/` → `site/javascripts/` post-build. When gzip_json is true, `load-gendoc-config.py` injects the script into `extra_javascript`. Zero changes needed in deploy.sh for the fetch wrapper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _headers, in-place gzip, and restore logic (D-01, D-02)</name>
  <files>gendoc-template/scripts/deploy.sh</files>
  <read_first>gendoc-template/scripts/deploy.sh</read_first>
  <action>
Remove from `deploy.sh`:

1. **The `_headers` write block** (lines 56-62): Delete the entire `cat > "$SITE_DIR/_headers"` through `echo "  Wrote _headers"` block. No `_headers` file is created.

2. **The in-place gzip block for search_index.json** (lines 65-72): Delete from the `SEARCH_INDEX=` variable declaration through `echo "  Gzipped search_index.json in-place"`. No special case for search_index.json.

3. **The `-path` exclusion in the find loop** (line 81): Change the find command from:
   ```bash
   find "$SITE_DIR" -name "*.json" -type f ! -name "*.json.gz" ! -path "*/search/search_index.json" -print0
   ```
   to:
   ```bash
   find "$SITE_DIR" -name "*.json" -type f ! -name "*.json.gz" -print0
   ```
   This includes ALL .json files uniformly.

4. **The ENTIRE restore section** (lines 98-116): Delete everything from `# ── Restore files for local dev` through the cleanup of temp files. No restore. The deploy_ok check and exit code handling move to immediately after the wrangler deploy block. The final `if ! $deploy_ok` block should be directly after the wrangler deploy success/failure handling — not after a restore section that no longer exists.

5. **The `deploy_ok` pattern**: Simplify. After the wrangler deploy, just:
   ```bash
   if ! $deploy_ok; then
       exit $exit_code
   fi
   ```
   No restore between deploy and exit.

Update the echo message at line 80 to include a count:
```bash
count=$(find "$SITE_DIR" -name "*.json" -type f ! -name "*.json.gz" | wc -l | tr -d ' ')
echo "  Gzipped and removed $count .json files"
```

Keep the `deploy_ok` pattern and exit code handling. Keep the PROJECT_NAME display (lines 43-50). Do NOT modify the wrangler deploy command line (the --branch change is Task 2).
  </action>
  <acceptance_criteria>
    - `grep -c "_headers" gendoc-template/scripts/deploy.sh` returns 0 (no _headers file creation anywhere)
    - `grep -c "raw.bak" gendoc-template/scripts/deploy.sh` returns 0 (no backup/restore of search_index.json)
    - `grep -c "search_index.json" gendoc-template/scripts/deploy.sh` returns 0 (no special case)
    - `grep "find.*json" gendoc-template/scripts/deploy.sh` shows the find command WITHOUT any `-path` exclusion for search_index
    - `grep -c "Restoring files" gendoc-template/scripts/deploy.sh` returns 0 (restore section removed)
    - `grep -c "gunzip" gendoc-template/scripts/deploy.sh` returns 0 (no restore via gunzip)
    - The gzip loop uses a count: `echo "  Gzipped and removed $count .json files"`
    - `grep -c "wrangler pages deploy" gendoc-template/scripts/deploy.sh` returns exactly 1
    - The `deploy_ok` / exit handling immediately follows the wrangler deploy block
    - `bash -n gendoc-template/scripts/deploy.sh` passes syntax check
  </acceptance_criteria>
  <verify>
    <automated>bash -n gendoc-template/scripts/deploy.sh && echo "Syntax OK" && ! grep -q "_headers\|raw.bak\|search_index.json\|Restoring files\|gunzip" gendoc-template/scripts/deploy.sh && echo "All removed" || (echo "FAIL: stale references remain" && exit 1)</automated>
  </verify>
  <done>deploy.sh contains no _headers, no raw.bak, no search_index.json special case, no restore section. All .json files are gzipped uniformly and permanently deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Add gendoc.yml-driven config for branch and gzip_json (D-03, D-05)</name>
  <files>gendoc-template/scripts/deploy.sh</files>
  <read_first>gendoc-template/scripts/deploy.sh</read_first>
  <action>
After the PROJECT_NAME read block (line 48), add YAML reads for two new values using the same `python3 -c "import yaml..."` pattern:

1. **`DEPLOY_BRANCH`** — reads `deploy.cloudflare.branch` from gendoc.yml, defaults to `"main"`:
```bash
DEPLOY_BRANCH=$(python3 -c "
import yaml, sys
with open(sys.argv[1], 'r') as f:
    cfg = yaml.safe_load(f)
print(cfg.get('deploy', {}).get('cloudflare', {}).get('branch', 'main'), end='')
" "$GENDOC_YML")
```

2. **`GZIP_JSON`** — reads `deploy.cloudflare.gzip_json` from gendoc.yml, defaults to `true`:
```bash
GZIP_JSON=$(python3 -c "
import yaml, sys
with open(sys.argv[1], 'r') as f:
    cfg = yaml.safe_load(f)
print(cfg.get('deploy', {}).get('cloudflare', {}).get('gzip_json', True), end='')
" "$GENDOC_YML")
```

**Wrap the gzip/delete loop** inside `if [ "$GZIP_JSON" = "True" ]; then ... fi`. When `GZIP_JSON` is not True, echo `"Skipping JSON gzip (deploy.cloudflare.gzip_json is not True)"`. The if block starts right after the `echo "Preparing files for deployment..."` line and ends before the `# ── Deploy using the wrangler.toml` comment. When gzip is skipped, deploy still runs — only the gzip/delete is bypassed.

**Replace hardcoded `--branch main`** on the wrangler command line with `--branch "$DEPLOY_BRANCH"`.

**No SW copy.** The fetch-gzip.js wrapper is deployed automatically by `copy-assets.py` (mirrors `javascripts/` → `site/javascripts/`). No deploy.sh changes needed for it.

Keep the PROJECT_NAME read block and the `deploy_ok` pattern unchanged.
  </action>
  <acceptance_criteria>
    - `grep "DEPLOY_BRANCH" gendoc-template/scripts/deploy.sh` finds the variable definition using `python3 -c "import yaml..."` with `deploy.cloudflare.branch` path
    - `grep "GZIP_JSON" gendoc-template/scripts/deploy.sh` finds the variable definition using `python3 -c "import yaml..."` with `deploy.cloudflare.gzip_json` path
    - The gzip/delete loop is enclosed in `if [ "$GZIP_JSON" = "True" ]; then ... fi`
    - When `GZIP_JSON` is not True, an echo message about skipping gzip is emitted
    - `grep "wrangler pages deploy" gendoc-template/scripts/deploy.sh` shows `--branch "$DEPLOY_BRANCH"` (not `--branch main`)
    - No SW copy or `search-gzip-sw.js` reference anywhere in the file
    - The PROJECT_NAME read block is preserved unchanged
    - `bash -n gendoc-template/scripts/deploy.sh` passes syntax check
  </acceptance_criteria>
  <verify>
    <automated>bash -n gendoc-template/scripts/deploy.sh && echo "Syntax OK" && grep -q 'DEPLOY_BRANCH' gendoc-template/scripts/deploy.sh && grep -q 'GZIP_JSON' gendoc-template/scripts/deploy.sh && grep -q '\$DEPLOY_BRANCH' gendoc-template/scripts/deploy.sh && echo "Config reads OK" && ! grep -q 'search-gzip-sw' gendoc-template/scripts/deploy.sh && echo "No SW copy"</automated>
  </verify>
  <done>deploy.sh reads deploy.cloudflare.branch and deploy.cloudflare.gzip_json from gendoc.yml, wraps gzip in the gzip_json toggle, passes branch to wrangler — no restore, no SW copy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gendoc.yml → deploy.sh | User-controlled YAML values (branch, gzip_json) consumed by shell script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | deploy.sh YAML reads | mitigate | Python yaml.safe_load prevents YAML code execution; branch value is shell-quoted with double quotes; gzip_json is compared to literal "True" not truthy |
| T-07-02 | Information Disclosure | deploy.sh echo output | accept | File paths echoed are relative to TEMPLATE_ROOT which is already known via wrangler.toml; no secrets exposed |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — deploy.sh uses existing python3 + yaml, gzip, find (all system tools) |
</threat_model>

<verification>
- `bash -n gendoc-template/scripts/deploy.sh` passes syntax check
- Visual review: no `_headers`, no `raw.bak`, no `search_index.json` special case, no `gunzip`, no restore
- Two new YAML reads (`branch`, `gzip_json`) present using the same `python3 -c` pattern as `PROJECT_NAME`
- `--branch "$DEPLOY_BRANCH"` on the wrangler line
- No `search-gzip-sw.js` reference anywhere
</verification>

<success_criteria>
deploy.sh is a single, unified script: all JSON files follow identical gzip→delete logic (no restore), branch and gzip_json come from gendoc.yml, and when gzip_json is false the entire gzip/delete cycle is skipped so raw .json deploys as-is.
</success_criteria>

<output>
Create `.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-01-SUMMARY.md` when done
</output>
