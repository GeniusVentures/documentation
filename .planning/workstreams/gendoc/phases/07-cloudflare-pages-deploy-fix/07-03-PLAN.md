---
phase: 07-cloudflare-pages-deploy-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - gendoc-template/gendoc.yml.example
  - gendoc.yml
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "gendoc-template/gendoc.yml.example documents deploy.cloudflare.branch and deploy.cloudflare.gzip_json with inline comments explaining their purpose"
    - "Host gendoc.yml includes deploy.cloudflare.branch field set to 'main'"
    - "Existing deploy.cloudflare fields (pages_project_name, production_branch, compatibility_date, custom_domain) are preserved unchanged"
  artifacts:
    - path: "gendoc-template/gendoc.yml.example"
      provides: "Template schema reference for all host projects"
      contains: "branch:"
      contains: "gzip_json"
    - path: "gendoc.yml"
      provides: "Host config with deploy branch read by deploy.sh"
      contains: "branch:"
  key_links:
    - from: "gendoc-template/scripts/deploy.sh"
      to: "gendoc.yml deploy.cloudflare.branch"
      via: "python3 yaml read"
      pattern: "deploy.*cloudflare.*branch"
    - from: "gendoc-template/scripts/deploy.sh"
      to: "gendoc.yml deploy.cloudflare.gzip_json"
      via: "python3 yaml read"
      pattern: "deploy.*cloudflare.*gzip_json"

---
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-CONTEXT.md
@gendoc-template/gendoc.yml.example
@gendoc.yml

<interfaces>
Current deploy.cloudflare section in host `gendoc.yml` (lines 86-91):
```yaml
deploy:
  cloudflare:
    pages_project_name: "gnus-ai-docs"
    compatibility_date: "2024-01-01"
    production_branch: "main"
    custom_domain: "docs.gnus.ai"
```

Current deploy.cloudflare section in `gendoc-template/gendoc.yml.example` (lines 86-93):
```yaml
deploy:
  cloudflare:
    pages_project_name: "myproject-docs"       # Wrangler pages project name
    production_branch: "main"                  # Git branch that triggers production deploy
    custom_domain: ""                          # Optional: custom domain (e.g. docs.example.com)
    compatibility_date: "2024-01-01"           # Cloudflare compatibility date
    # Authenticate once with: wrangler login
```

`production_branch` is used by setup.sh for `wrangler.toml` generation (Cloudflare Pages CI trigger). The new `branch` field is for `deploy.sh`'s `wrangler pages deploy --branch` flag. Both fields should coexist — they serve different purposes.

The new `gzip_json` field is deploy.sh-only (D-03) — consumers do not read it. It controls whether deploy.sh runs the gzip/delete/restore cycle. Default: `true`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gendoc-template/gendoc.yml.example with branch and gzip_json fields (D-03, D-05)</name>
  <files>gendoc-template/gendoc.yml.example</files>
  <read_first>gendoc-template/gendoc.yml.example</read_first>
  <action>
Add two new fields to the `deploy.cloudflare` block in `gendoc-template/gendoc.yml.example`. Insert them between the `production_branch` line and the `custom_domain` line, maintaining alphabetical-ish grouping.

1. `branch: "main"` — the branch alias passed to `wrangler pages deploy --branch`. Add the comment: `# Branch alias for wrangler pages deploy (default "main")`. This is separate from `production_branch` which configures the Cloudflare Pages CI trigger in wrangler.toml.

2. `gzip_json: true` — controls whether deploy.sh gzips .json files before upload. Add the comment: `# Gzip .json files before deploy (Cloudflare Pages 25 MiB per-file limit). Set false if not using Cloudflare Pages.`

The updated deploy.cloudflare section should read (in order):
- `pages_project_name`
- `production_branch`
- `branch` (NEW)
- `gzip_json` (NEW)
- `custom_domain`
- `compatibility_date`

Do NOT change existing field names, values, or comments. Do NOT remove `production_branch`. Do NOT change the comment structure of existing fields. The comment on `display: "myproject-docs"` uses `# Wrangler pages project name` style — match this single-line comment style for the new fields.
  </action>
  <acceptance_criteria>
    - `grep "branch:" gendoc-template/gendoc.yml.example` returns a line in the deploy.cloudflare section with value `"main"` and a comment about `wrangler pages deploy`
    - `grep "gzip_json:" gendoc-template/gendoc.yml.example` returns a line in the deploy.cloudflare section with value `true` and a comment about `25 MiB` or `Cloudflare Pages`
    - `production_branch` still exists in the file — `grep -c "production_branch:" gendoc-template/gendoc.yml.example` returns 1
    - `pages_project_name` still exists and is unchanged
    - `custom_domain` still exists and is unchanged
    - `compatibility_date` still exists and is unchanged
    - The fields appear in this order: pages_project_name, production_branch, branch, gzip_json, custom_domain, compatibility_date — verify with `grep -n "pages_project_name\|production_branch\|branch:\|gzip_json\|custom_domain\|compatibility_date" gendoc-template/gendoc.yml.example`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; cfg=yaml.safe_load(open('gendoc-template/gendoc.yml.example')); dc=cfg['deploy']['cloudflare']; assert dc['branch']=='main'; assert dc['gzip_json']==True; assert dc['production_branch']=='main'; print('YAML parse OK, all fields present')"</automated>
  </verify>
  <done>gendoc.yml.example deploy.cloudflare section includes branch and gzip_json with descriptive comments, all existing fields preserved, YAML parses correctly</done>
</task>

<task type="auto">
  <name>Task 2: Update host gendoc.yml with deploy.cloudflare.branch (D-05)</name>
  <files>gendoc.yml</files>
  <read_first>gendoc.yml</read_first>
  <action>
Add the `branch` field to the `deploy.cloudflare` section in the host `gendoc.yml`. Insert `branch: "main"` on a new line between `production_branch: "main"` and `custom_domain: "docs.gnus.ai"`. No comment needed — the host gendoc.yml uses a minimal style (only the `# ──` section header comments, no per-field inline comments). Match the existing style: no trailing comments, bare key-value pairs.

Do NOT add `gzip_json` to the host gendoc.yml — the host inherits the default `true` from deploy.sh's YAML read. Only `branch` is needed because the default `"main"` should be explicit for production deploys.

Do NOT modify any other sections. The `production_branch` field must remain unchanged.
  </action>
  <acceptance_criteria>
    - `grep "branch:" gendoc.yml` returns a line in the deploy.cloudflare section with value `"main"` — `grep -A 10 "cloudflare:" gendoc.yml | grep "branch:"` returns `branch: "main"`
    - `production_branch: "main"` still exists in the deploy.cloudflare section
    - `pages_project_name: "gnus-ai-docs"` is unchanged
    - `custom_domain: "docs.gnus.ai"` is unchanged
    - `compatibility_date: "2024-01-01"` is unchanged
    - The fields appear in order: pages_project_name, compatibility_date, production_branch, branch, custom_domain — verify with `grep -n "pages_project_name\|compatibility_date\|production_branch\|branch:\|custom_domain" gendoc.yml`
    - `python3 -c "import yaml; cfg=yaml.safe_load(open('gendoc.yml')); print(cfg['deploy']['cloudflare']['branch'])"` outputs `main`
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; cfg=yaml.safe_load(open('gendoc.yml')); assert cfg['deploy']['cloudflare']['branch']=='main'; print('branch field OK')"</automated>
  </verify>
  <done>Host gendoc.yml deploy.cloudflare section includes branch: "main", all existing fields preserved, YAML parses correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gendoc.yml → deploy.sh | User-controlled YAML values consumed by shell script — covered in Plan 01's threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | gendoc.yml.example comments | accept | Comments document public configuration schema — no secrets, no credentials |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — YAML file edits only |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('gendoc-template/gendoc.yml.example'))"` parses without error
- `python3 -c "import yaml; yaml.safe_load(open('gendoc.yml'))"` parses without error
- Both files have `branch` in the deploy.cloudflare section with value `"main"`
- gendoc.yml.example has `gzip_json: true` in deploy.cloudflare
</verification>

<success_criteria>
Template schema (gendoc.yml.example) and host config (gendoc.yml) both include the new deploy.cloudflare fields. deploy.sh can read them with zero changes to the YAML-reading code.
</success_criteria>

<output>
Create `.planning/workstreams/gendoc/phases/07-cloudflare-pages-deploy-fix/07-03-SUMMARY.md` when done
</output>
