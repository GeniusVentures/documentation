---
context: phase
phase: 08-llm-document-graph
workstream: gendoc
task: 0
total_tasks: 8
total_plans: 3
status: ready_to_execute
last_updated: 2026-07-25T01:19:50.798Z
---

# Continue Here — Phase 8: LLM Document Graph

<current_state>
Phase 8 is fully planned with 3 executable plans in 2 waves. Zero tasks executed — all work is in the planning stage. The discussion phase captured 9 implementation decisions (D-01 through D-09) in CONTEXT.md. Plans were revised through the plan-checker feedback loop (5 warnings → 2 minor remaining, 1 accepted).

**Wave 1** (parallel):
- Plan 08-01: build-graph.py — Python script with two-pass LLM pipeline, section-level analysis, incremental reanalysis, provider-agnostic API key detection
- Plan 08-02: Worker TypeScript integration — CatalogEntry graph types, llms-meta.json loading, configurable weighted scoring with graph support and redundancy penalty

**Wave 2** (depends on 08-01):
- Plan 08-03: Build pipeline integration — build.sh Step 9, deploy.yml secrets, build.yml graph step
</current_state>

<completed_work>

## Prior Session (before compaction)
- Applied U-shaped interleaved document ordering to `gendoc-template/ask-ai/worker/src/index.ts`
  - Primary doc fallback, interleaved supporting docs (odds-from-end + evens-from-start), swap for strongest-docs-adjacent
- Ingested `llm-doc-graph.md` SPEC → Phase 8 in gendoc workstream
- Discuss Phase 8 (assumptions + full) → 9 implementation decisions captured
- Plan Phase 8 → 3 plans created, verified, revised (5 checker warnings → resolved)

## This Session
- No new work executed — session was a continuation review
</completed_work>

<remaining_work>

## Wave 1 — Plans 08-01 and 08-02 (independent, parallel)
- [ ] **Plan 08-01**: Create `gendoc-template/scripts/build-graph.py`
  - Task 1: Section extraction and content hashing
  - Task 2: Two-pass LLM pipeline (Pass 1: fact extraction with evidence offsets, Pass 2: interpretation)
  - Covers: LLMGRAPH-01, LLMGRAPH-03, LLMGRAPH-04, LLMGRAPH-06, LLMGRAPH-07, LLMGRAPH-08, LLMGRAPH-09, LLMGRAPH-10
- [ ] **Plan 08-02**: Worker TypeScript integration
  - Task 1: Extend `types.ts` — GraphMetadata, MetadataOrigin, MetadataValue types
  - Task 2: Load graph metadata in `catalog.ts` — parse llms-meta.json
  - Task 3: Enhanced `scoreEntries` in `utils.ts` — configurable weights, graph support G(d,q), redundancy penalty D(d,S)
  - Task 4: Behavioral test for scoring
  - Covers: LLMGRAPH-02 (types), LLMGRAPH-05 (scoring)

## Wave 2 — Plan 08-03 (depends on 08-01)
- [ ] **Plan 08-03**: Build pipeline wiring
  - Task 1: Add build-graph.py invocation to build.sh (Step 9, after build-llms.py)
  - Task 2: Add API key secrets + env passthrough to deploy.yml and conditional graph step to build.yml
</remaining_work>

<decisions_made>

All 9 implementation decisions are captured in `08-CONTEXT.md`. Key decisions:
- **D-01/D-02**: Both OpenAI + Anthropic, prompts inline in Python script
- **D-03/D-04**: Extend `llms-meta.json`, content hashes alongside metadata per section
- **D-05**: All 5 graph relation types in one phase
- **D-06**: `CatalogEntry` consumes `llms-meta.json`
- **D-07**: Script gates on API key from `.env`/env var, skips silently when absent
- **D-08**: Invoked from `build.sh` after `build-llms.py`
- **D-09**: CI/CD via existing reusable workflow pattern
</decisions_made>

<blockers>
None — plans are verified and ready to execute.
</blockers>

## Required Reading (in order)

1. `.planning/workstreams/gendoc/phases/08-llm-document-graph/08-CONTEXT.md` — All 9 implementation decisions, canonical references, integration points
2. `.planning/workstreams/gendoc/phases/08-llm-document-graph/08-01-PLAN.md` — build-graph.py plan (read before executing)
3. `.planning/workstreams/gendoc/phases/08-llm-document-graph/08-02-PLAN.md` — Worker TypeScript plan (read before executing)
4. `.planning/workstreams/gendoc/phases/08-llm-document-graph/08-03-PLAN.md` — Build pipeline plan (read before executing)
5. `.planning/workstreams/gendoc/llm-doc-graph.md` — Full architecture SPEC (485 lines)
6. `gendoc-template/scripts/build-llms.py` — Pattern to follow for build-graph.py
7. `gendoc-template/ask-ai/worker/src/types.ts` — CatalogEntry type to extend
8. `gendoc-template/ask-ai/worker/src/catalog.ts` — scoreEntries to enhance

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Do not reverse/reorder documents before budget consumption. The rule is "select first, reorder second" — stronger docs must consume the character budget before weaker ones.
- **ANTI-PATTERN**: Do not create a separate `llms-graph.json` file. Extend `llms-meta.json` per D-03.
- **ANTI-PATTERN**: Do not gate build.sh invocation on API key presence. The script handles that internally (D-07). Only gate on script file existence.

## Infrastructure State

- Working directory: `/Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/documentation`
- Submodule: `gendoc-template` at `../gendoc-template`
- Git branch: `main` (clean, aside from uncommitted planning artifacts)
- No running services or background processes

<context>
All planning is complete. The next step is execution. Wave 1 (08-01 + 08-02) can run in parallel — build-graph.py and worker TypeScript are independent. After 08-01 completes, Wave 2 (08-03) wires the script into build.sh and CI/CD workflows.

The U-shaped context ordering improvement is already applied to index.ts (separate from Phase 8). Phase 8 focuses exclusively on build-time metadata enrichment that will feed into better relevance scoring.
</context>

<next_action>
Start with: `/gsd:execute-phase 08 --ws gendoc`

This executes all 3 plans in 2 waves. Wave 1 runs 08-01 and 08-02 in parallel. Wave 2 runs 08-03 after 08-01 completes.
</next_action>
